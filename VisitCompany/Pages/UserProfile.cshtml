@page
@model VisitCompany.Pages.UserProfileModel
@{
    Layout = "Shared/_AdminLayout";
    ViewData["title"] = "مدیریت نقش‌ها";
}




<h4>
    <center>  کاربر گرامی جناب اقای @Model.Accounts.Fullname با نام کاربری @Model.Accounts.UserName  خوش آمدید. </center>
</h4>

<h4> شماره تلفن همراه شما: @Model.Accounts.Mobile </h4> 
<br/>
<h4> تاریخ  ورود قبلی شما: @Model.Accounts.PreviousLogin</h4>

<br/>

<div>
    <select asp-for="SelectedCompanyId" asp-items="Model.Companies" class="form-control">
        <option value="">لیست شرکت هایی که شما مجاز به ارزیابی هستید</option>
    </select>
</div>

<h4>تمامی  ارزیابی های صورت گرفته توسط شما (مرتب شده از آخر به اول)    </h4>

<div class="row">
<div class="col-md-12">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">لیست ارزیابی های صورت گرفته توسط شما  (@Model.CheckLists.Count)</h3>
        </div>
        <h4>انتخاب شرکت</h4>

   

        <div class="panel-body">
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <table id="datatable" class="table table-striped table-bordered">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>عنوان</th>
                            <th>نام فرد مسئول حاضر در کسب و کار</th>
                            <th>مسئولیت فرد</th>
                            <th>شماره تماس</th>
                                    
                            <th>نام شرکت</th>
                            <th>تاریخ ارزیابی </th>
                                   
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in Model.CheckLists)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Title</td>
                                <td>@item.NamePeopleCo</td>
                                <td>@item.RspponsePeopleCo</td>
                                <td>@item.PhonePeopleCo</td>
                                <td>@item.Company</td>
                                <td>@item.CreattionDate</td>
                                      
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
</div>
